#!/bin/bash

sudo npm install -g karma-cli

sudo npm install --save-dev karma karma-mocha karma-chai karma-phantomjs-launcher

karma init

sudo npm install --save angular
sudo npm install --save-dev angular-mocks
